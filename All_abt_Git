1)Install Git:
sudo apt update
sudo apt install git

2)Installing git-gui on Linux:
sudo apt update
sudo apt install git-gui

After installation, you can verify if git-gui is installed correctly by running:
git gui

3)Commit and push code:
add in origin:
git remote add origin https://<your key>github.com/<username>/<repository>.git

Set origin:
git remote set-url origin https://<your key>github.com/<username>/<repository>.git

view all remotes:
git remote -v

remove origin:
git remote remove origin

commit on git gui:
git gui

push code to git
git push origin main

